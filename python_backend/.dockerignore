.venv/
__pycache__/
*.pyc
.env
data/
outputs/
# `artifacts/` is deliberately NOT excluded: the fitted model ships in the image so a
# release carries a known-good bundle. Excluding it was correct while the plan was to
# mount a volume and train after deploy.
*.md
.git
