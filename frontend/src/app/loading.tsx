// todo: skeletons per page. a spinner for a dashboard of twelve cards reads as broken.
export default function Loading() {
  return <div className="p-6 text-sm text-muted-foreground">Loading…</div>;
}
